Node.js + Yeoman installation:
# requires NVM installed properly and not as root

$ npm install -g yo
$ mkdir site
$ cd site
$ npm install -g generator-webapp
$ yo webapp

Add droplet ip address to hosts file:
$ nano /etc/hosts

Access website at localhost:9000

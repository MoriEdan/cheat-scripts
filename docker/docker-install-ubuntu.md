Docker Ubuntu install cheats
============================

<h4>Ubuntu 14.04 LTS</h4>
<pre>
$ sudo apt-get update
$ sudo apt-get install docker.io
$ sudo ln -sf /usr/bin/docker.io /usr/local/bin/docker

#test the install
$ docker -v
</pre>
